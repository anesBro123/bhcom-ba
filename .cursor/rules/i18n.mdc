---
description: Translation keys and ngx-translate
globs: src/**/*.{ts,html},public/assets/*.json
alwaysApply: false
---

# i18n

- All user-visible strings via translation keys (`labelKey`, `titleKey`, `| translate`).
- Add every new key to **both** `public/assets/en.json` and `public/assets/bh.json` (single file per language).
- In TS, return key strings for labels (e.g. `statusLabel()` → `'portal.user.features.freight.table.cargoTypes.${type}'`).
- Do not embed English prose in templates when a key exists or should exist.

## Key tree (mirrors `src/app/` layers)

```
shared.*                          — shell chrome + form/table/confirm frameworks
guest.*                           — public pages (landing, sign-in, login, register)
portal.admin.pages.*              — admin route page header ({ title, subtitle })
portal.admin.features.<feature>.* — admin feature (form, table)
portal.user.nav.*                 — user nav labels (marketplace, our listings)
portal.user.pages.*               — user route page header ({ title, subtitle }; detail subtitles: `listingDetail.subtitleSearch` / `.subtitleOur`)
portal.user.features.<domain>.<feature>.* — user feature (form, table)
```

### Shared (`shared.*`)

| Path | Use for |
|------|---------|
| `shared.topbar.*` | Portal + shared topbar widgets (theme toggle, language toggle, notifications, account, create menu) |
| `shared.form.common.*` | Form footer (`previous`, `next`), stepper pill aria (`validationState.*`), leave-form discard dialog (`discardChanges.*`), field errors (`errors.*` incl. `minDate`, `dateRange`) |
| `shared.table.common.*` | Table loading, empty, emptyFiltered, pagination, row actions |
| `shared.table.filters.*` | Filter flyout toggle, clear (`clearShort`, `clearAll`, `clearSelection`), expand/collapse, chips, option tiles, multi-select, sliders, units (`kg`, `m²`, `m`), **`period`** (default date-range filter label) |
| `shared.table.view.*` | Desktop list/card view toggle `aria-label`s (`switchToCard`, `switchToList`) |
| `shared.route.*` | Shared route UI copy (`fromTo` aria-label for `RouteDisplayComponent`) |
| `shared.vehicle.*` | Shared vehicle UI copy (`namePlate` aria-label for `VehicleDisplayComponent`) |
| `shared.warehouse.*` | Shared warehouse UI copy (`nameCity` aria-label for `WarehouseDisplayComponent`) |
| `shared.dateRange.*` | Shared date range **display** copy (`fromTo`, `single` aria-labels for `DateRangeDisplayComponent`) |
| `shared.datePeriod.*` | Shared date period **picker** copy (placeholder, clear, months, weekdays, nav aria) — `DatePeriodPickerComponent` |
| `shared.entityStatus.*` | Shared status pill labels for `StatusBadgeComponent` (interim — user transport/freight/warehouse); per-entity keys later under `portal.*.features.<entity>.status.*` |
| `shared.common.*` | Shared yes/no labels (`booleanYes`, `booleanNo`) — boolean filter option tiles |
| `shared.confirm.common.*` | Confirmation dialog cancel/confirm button labels (defaults; do not duplicate per feature) |

Table **merged period** column headers: `portal.user.features.transport.table.columns.period`, `portal.user.features.warehouse.table.columns.period` (distinct from form field keys like `transportStartDate`).

Table **filter labels**: `portal.user.features.<entity>.table.filters.*` and `portal.admin.features.<entity>.table.filters.*`. Legacy `…table.filterGroups.*` keys may remain on defs but are not shown in flyout UI. Admin examples: user permissions + DOB; vehicle `vehicleType` / `vrstaGoriva` option tiles + four registration date filter titles; warehouse city `multiSelect` + facility booleans via `BOOLEAN_FILTER_OPTIONS`. Vehicle date filters use entity-specific `…table.filters.<field>` titles; user DOB uses `shared.table.filters.period`.

### Guest (`guest.*`)

| Path | Use for |
|------|---------|
| `guest.signIn.*` | Portal picker at `/` |
| `guest.login.*` | User + admin login pages |
| `guest.register.*` | Company registration page chrome |
| `guest.register.form.*` | Register company stepper form |
| `guest.topbar.*` | App topbar (guest mode) |

### Portal admin feature example

```
portal.admin.features.vehicles.form.fields.marka
portal.admin.features.vehicles.table.columns.vehicle
portal.admin.features.vehicles.table.filters.vehicleType
portal.admin.features.vehicles.table.filters.vrstaGoriva
portal.admin.features.vehicles.table.filters.vazenjeRegistracije
portal.admin.features.vehicles.table.deleteConfirm.title
portal.admin.features.users.table.filters.canCreateRoute
portal.admin.features.warehouses.table.filters.city
portal.admin.nav.createVehicle
portal.admin.pages.home.title
portal.admin.pages.vehicles.title
portal.admin.pages.vehicles.subtitle
portal.admin.pages.createVehicle.title
```

User topbar: `shared.topbar.offer.openMenu` for user create menu; `shared.topbar.create.openMenu` for admin create menu; account **My listings** (`shared.topbar.account.profile` mirrors `portal.user.nav.ourListings`) → `userOurListingsUrl()`. Detail back-nav: `portal.user.nav.search` (Marketplace / Tržište) or `portal.user.nav.ourListings` (My listings / Moje objave).

### User portal vocabulary (EN / BS)

| Concept | EN keys | BS labels |
|---------|---------|-----------|
| Marketplace hub | `pages.home.*`, `nav.search` | Tržište |
| My listings hub | `pages.ourListings.*`, `nav.ourListings` | Moje objave |
| Detail subtitle (other company) | `pages.listingDetail.subtitleSearch` | Pregledajte objavu druge kompanije. |
| Detail subtitle (your company) | `pages.listingDetail.subtitleOur` | Upravljajte objavom vaše kompanije. |

Hub subtitles use parallel verb + **other company** vs **your company** — do not repeat entity types (tabs cover transport/freight/warehouse).

### Portal user feature example

```
portal.user.features.freight.form.sections.route.title
portal.user.features.freight.form.sections.freight.title
portal.user.features.entityTabs.transport
portal.user.features.entityTabs.ariaLabel
portal.user.nav.search
portal.user.nav.ourListings
portal.user.pages.home.title
portal.user.pages.home.subtitle
portal.user.pages.ourListings.title
portal.user.pages.ourListings.subtitle
portal.user.pages.listingDetail.subtitleSearch
portal.user.pages.listingDetail.subtitleOur
shared.topbar.account.profile
shared.topbar.offer.openMenu
shared.topbar.create.openMenu
portal.user.pages.createTransport.title
```

**City fields:** municipality names in `BIH_CITY_OPTIONS` use plain `label` strings — no per-city i18n keys.

## Rules for new keys

1. **Do not** add feature strings under legacy roots (`forms.*`, `tables.*`, top-level `nav.*`, `pages.*`, `landing.*`, `login.*`).
2. **Admin vs user** — always under `portal.admin` or `portal.user` (never cross-import keys).
3. **Form defs** (`*.form.ts`) — keys under `…features.<feature>.form.*`.
4. **Table defs** (`*.table.ts`) — keys under `…features.<feature>.table.*` including `title` + `subtitle`; use `shared.table.common.*` for shared table chrome. Destructive row actions: add `…table.deleteConfirm.title` / `…message` in the feature namespace; button labels use `shared.confirm.common.*` (see `confirm.mdc`).
5. **Route `data`** — `titleKey: 'portal.<portal>.pages.<pageId>.title'` (optional; page templates usually own title keys via `PageTitleComponent`).
6. **Page subtitles** — every portal page needs a subtitle under `portal.*.pages.<id>.subtitle`. Page components own `pageTitleKey` / `pageSubtitleKey` — not table or form defs. Write helpful UX copy — do not repeat the title.
7. **Nav labels** — user publish CTAs: `portal.user.pages.createTransport|createFreight|createWarehouse.title`; topbar create menus: `shared.topbar.offer.openMenu` (user), `shared.topbar.create.openMenu` (admin); account my listings → our listings (`shared.topbar.account.profile` mirrors `portal.user.nav.ourListings`); admin create labels: `portal.admin.nav.create*`.
8. **Enum/select options** — colocate under the same feature subtree (e.g. `…form.vrstaVozila.putnicko`).
9. **Entity status labels** — interim shared union: `shared.entityStatus.*`; when an entity gets its own statuses, add `portal.<portal>.features.<entity>.status.<value>` — see `entity-status.mdc`.

## Files

- Translations: `public/assets/en.json`, `public/assets/bh.json`
- Language switching: `shared/core/i18n/language.service.ts` — `AppLanguage` (`'en'` \| `'bh'`), `setLanguage()`, `toggle()`; persists `bhcom-ba-language`; sets `document.documentElement.lang` to `en` or `bs`.
- Topbar language toggle aria: `shared.topbar.lang.switchToEn`, `shared.topbar.lang.switchToBh` (button shows active code **EN** / **BS**).
