import type { TopbarNavItem } from '../common/models/portal-config.model';

/** User portal: browse via brand → home; offer via topbar + icon menu. */
export const USER_TOPBAR_NAV: TopbarNavItem[] = [];
