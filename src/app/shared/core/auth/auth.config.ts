export const AUTH_STORAGE_KEY = 'bhcom.session';

/** Replace stub with HttpClient POST to these paths when API is ready. */
export const AUTH_API = {
  userLoginPath: '/auth/login',
  adminLoginPath: '/auth/admin/login',
} as const;
